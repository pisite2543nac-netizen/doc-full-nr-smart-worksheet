import { Download,Filter,Plus,Search } from 'lucide-react';import { EmptyState } from './ui';
export function AdminTablePage({title,description,columns,rows,addLabel='เพิ่มข้อมูล'}:{title:string;description:string;columns:string[];rows:(string|number)[][];addLabel?:string}){return <div><div className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="page-title">{title}</h1><p className="muted mt-1">{description}</p></div><div className="flex gap-2"><button className="btn-secondary"><Download size={17}/>Export</button><button className="btn-primary"><Plus size={17}/>{addLabel}</button></div></div><div className="card mt-6 flex flex-col gap-3 md:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 text-slate-400" size={18}/><input className="field pl-10" placeholder="ค้นหา..."/></div><button className="btn-secondary"><Filter size={17}/>ตัวกรอง</button></div>{rows.length===0?<div className="mt-5"><EmptyState/></div>:<div className="mt-5 overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-50 text-slate-600"><tr>{columns.map(c=><th className="px-4 py-3 font-semibold" key={c}>{c}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr className="border-t border-slate-100 hover:bg-slate-50" key={i}>{r.map((v,j)=><td className="px-4 py-3" key={j}>{v}</td>)}</tr>)}</tbody></table></div></div>}</div>}
