import { ArrowLeft,Mail } from 'lucide-react';import { FormEvent,useState } from 'react';import { Link } from 'react-router-dom';import { useAuth } from '../../context/AuthContext';
export function ForgotPasswordPage(){const {reset,demoMode}=useAuth();const [email,setEmail]=useState('');const [sent,setSent]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(!demoMode)try{await reset(email)}catch{}setSent(true)}return <div className="grid min-h-screen place-items-center p-4"><div className="card w-full max-w-md"><Link to="/login" className="mb-5 inline-flex items-center gap-2 text-sm font-semibold text-blue-600"><ArrowLeft size={17}/>กลับไปหน้าเข้าสู่ระบบ</Link><div className="grid h-12 w-12 place-items-center rounded-2xl bg-blue-100 text-blue-700"><Mail/></div><h1 className="mt-4 text-2xl font-bold">ลืมรหัสผ่าน</h1><p className="muted mt-2">กรอกอีเมลเพื่อรับคำแนะนำสำหรับตั้งรหัสผ่านใหม่</p>{sent?<div className="mt-6 rounded-xl bg-green-50 p-4 text-sm text-green-800">หากอีเมลนี้ลงทะเบียนไว้ ระบบจะส่งคำแนะนำสำหรับตั้งรหัสผ่านใหม่</div>:<form onSubmit={submit} className="mt-6 space-y-4"><div><label className="label">อีเมล</label><input className="field" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><button className="btn-primary w-full">ส่งลิงก์รีเซ็ตรหัสผ่าน</button></form>}</div></div>}
