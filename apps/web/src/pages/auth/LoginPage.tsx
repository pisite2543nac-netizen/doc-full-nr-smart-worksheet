import { Eye,EyeOff,GraduationCap,ShieldCheck } from 'lucide-react';import { FormEvent,useState } from 'react';import { Link,useNavigate } from 'react-router-dom';import { useAuth } from '../../context/AuthContext';
export function LoginPage(){const {login,loginDemo,demoMode}=useAuth();const n=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [show,setShow]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');if(!/^\S+@\S+\.\S+$/.test(email)){setError('กรุณากรอกอีเมลให้ถูกต้อง');return;}setBusy(true);try{const u=await login(email,password);n(u.role==='admin'?'/admin':'/user')}catch{setError('ไม่สามารถเข้าสู่ระบบได้ กรุณาตรวจสอบข้อมูลหรือติดต่อผู้ดูแลระบบ')}finally{setBusy(false)}}function demo(role:'admin'|'user'){loginDemo(role);n(role==='admin'?'/admin':'/user')}return <div className="grid min-h-screen lg:grid-cols-2"><section className="hidden bg-gradient-to-br from-blue-700 to-blue-500 p-12 text-white lg:flex lg:flex-col lg:justify-between"><div><div className="flex items-center gap-3"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-white/15"><GraduationCap/></div><b className="text-xl">Nangrong Smart Worksheet</b></div><div className="mt-24 max-w-xl"><h1 className="text-5xl font-bold leading-tight">จัดการใบงาน<br/>ง่ายขึ้นในที่เดียว</h1><p className="mt-5 text-lg text-blue-100">รายวิชา ใบงาน Digital/Paper การสแกน ส่งงาน ตรวจงาน และรายงาน</p></div></div><div className="flex items-center gap-2 text-sm text-blue-100"><ShieldCheck/>ออกแบบโดยคำนึงถึงสิทธิ์และความปลอดภัยของผู้เรียน</div></section><section className="flex items-center justify-center p-5"><div className="w-full max-w-md"><div className="mb-8 lg:hidden"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-blue-600 text-white"><GraduationCap/></div></div><h1 className="text-3xl font-bold">เข้าสู่ระบบ</h1><p className="mt-2 text-slate-500">ระบบจัดการใบงานสำหรับครูและนักเรียน</p>{demoMode&&<div className="mt-5 rounded-2xl border border-amber-200 bg-amber-50 p-4"><b className="text-amber-900">โหมดทดลอง UX/UI</b><p className="mt-1 text-sm text-amber-800">GitHub Pages จะเปิดโหมดนี้อัตโนมัติ ไม่เชื่อมข้อมูลจริง</p><div className="mt-3 grid grid-cols-2 gap-2"><button className="btn-secondary" onClick={()=>demo('admin')}>ทดลอง Admin</button><button className="btn-secondary" onClick={()=>demo('user')}>ทดลอง User</button></div></div>}<form className="mt-7 space-y-4" onSubmit={submit}><div><label className="label">อีเมล</label><input className="field" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@example.com" autoComplete="email"/></div><div><label className="label">รหัสผ่าน</label><div className="relative"><input className="field pr-11" type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/><button type="button" onClick={()=>setShow(v=>!v)} className="absolute right-3 top-2.5 text-slate-500" aria-label="แสดงหรือซ่อนรหัสผ่าน">{show?<EyeOff/>:<Eye/>}</button></div></div>{error&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">⚠️ {error}</p>}<div className="flex items-center justify-between text-sm"><label className="flex items-center gap-2"><input type="checkbox"/>จดจำการเข้าสู่ระบบ</label><Link className="font-semibold text-blue-600" to="/forgot-password">ลืมรหัสผ่าน</Link></div><button className="btn-primary w-full" disabled={busy||demoMode}>{busy?'กำลังเข้าสู่ระบบ...':'เข้าสู่ระบบ'}</button></form><p className="mt-6 text-center text-xs text-slate-500">หากไม่สามารถเข้าสู่ระบบได้ กรุณาติดต่อ Admin</p></div></section></div>}
