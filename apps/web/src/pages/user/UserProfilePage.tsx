import { Camera,KeyRound,LogOut,UserCircle2 } from 'lucide-react';import { useAuth } from '../../context/AuthContext';
export function UserProfilePage(){const {user}=useAuth();return <div className="mx-auto max-w-3xl"><h1 className="page-title">โปรไฟล์</h1><div className="card mt-6"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="relative"><div className="grid h-24 w-24 place-items-center rounded-full bg-slate-100 text-slate-400"><UserCircle2 size={52}/></div><button className="absolute bottom-0 right-0 rounded-full bg-blue-600 p-2 text-white" aria-label="เปลี่ยนรูป"><Camera size={16}/></button></div><div><h2 className="text-xl font-bold">{user?.displayName}</h2><p className="muted">{user?.studentId} · {user?.classroomName}</p><p className="muted">{user?.email}</p></div></div><div className="mt-7 grid gap-4 sm:grid-cols-2"><div><label className="label">ชื่อแสดงผล</label><input className="field" defaultValue={user?.displayName}/></div><div><label className="label">อีเมล</label><input className="field bg-slate-50" disabled defaultValue={user?.email}/></div><div><label className="label">รหัสนักเรียน/นักศึกษา</label><input className="field bg-slate-50" disabled defaultValue={user?.studentId}/></div><div><label className="label">ห้องเรียน</label><input className="field bg-slate-50" disabled defaultValue={user?.classroomName}/></div></div><div className="mt-7 flex flex-wrap gap-2 border-t border-slate-200 pt-5"><button className="btn-secondary"><KeyRound size={17}/>เปลี่ยนรหัสผ่าน</button><button className="btn-secondary text-red-600"><LogOut size={17}/>ออกจากระบบ</button></div></div></div>}
