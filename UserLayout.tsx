import { Home,FileText,ScanLine,User,LogOut,GraduationCap } from 'lucide-react';import { NavLink,Outlet,useNavigate } from 'react-router-dom';import { useAuth } from '../context/AuthContext';
const nav=[['/user','หน้าหลัก',Home],['/user/worksheets','ใบงาน',FileText],['/user/scan','สแกน',ScanLine],['/user/profile','โปรไฟล์',User]] as const;
export function UserLayout(){const {user,logout,demoMode}=useAuth();const n=useNavigate();return <div className="min-h-screen bg-slate-50 pb-20 md:pb-0"><header className="sticky top-0 z-30 border-b border-slate-200 bg-white"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-xl bg-blue-600 text-white"><GraduationCap size={21}/></div><div><b>Smart Worksheet</b><div className="hidden text-xs text-slate-500 sm:block">{user?.classroomName??'นักเรียน/นักศึกษา'}</div></div></div><nav className="hidden items-center gap-1 md:flex">{nav.map(([to,label,Icon])=><NavLink end={to==='/user'} key={to} to={to} className={({isActive})=>`flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-semibold ${isActive?'bg-blue-50 text-blue-700':'text-slate-600'}`}><Icon size={18}/>{label}</NavLink>)}<button className="ml-2 rounded-xl p-2 text-red-600 hover:bg-red-50" aria-label="ออกจากระบบ" onClick={async()=>{await logout();n('/login')}}><LogOut size={19}/></button></nav>{demoMode&&<span className="badge bg-amber-100 text-amber-800 md:hidden">Demo</span>}</div></header><main className="mx-auto max-w-7xl p-4 md:p-7"><Outlet/></main><nav className="fixed inset-x-0 bottom-0 z-40 grid grid-cols-4 border-t border-slate-200 bg-white md:hidden">{nav.map(([to,label,Icon])=><NavLink end={to==='/user'} key={to} to={to} className={({isActive})=>`flex flex-col items-center gap-1 py-2 text-xs font-semibold ${isActive?'text-blue-600':'text-slate-500'}`}><Icon size={21}/><span>{label}</span></NavLink>)}</nav></div>}
