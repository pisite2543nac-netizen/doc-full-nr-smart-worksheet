import { useAuth } from '../../context/AuthContext';export function AdminProfilePage(){const {user}=useAuth();return <div className="mx-auto max-w-2xl"><h1 className="page-title">โปรไฟล์ Admin</h1><div className="card mt-6"><div><label className="label">ชื่อแสดงผล</label><input className="field" defaultValue={user?.displayName}/></div><div className="mt-4"><label className="label">อีเมล</label><input className="field bg-slate-50" disabled defaultValue={user?.email}/></div><button className="btn-primary mt-5">บันทึก</button></div></div>}
