import { onCall } from 'firebase-functions/v2/https';import { db } from '../shared/firebase';import { requireAdmin } from '../shared/auth';import { audit } from '../shared/audit';const region='asia-southeast1';
const esc=(v:unknown)=>`"${String(v??'').replaceAll('"','""')}"`;
export const exportReport=onCall({region,timeoutSeconds:120},async req=>{const a=await requireAdmin(req);const {type='submissions',limit=1000}=req.data as any;const snap=await db.collection('submissions').limit(Math.min(Number(limit)||1000,5000)).get();const rows=[['submissionId','worksheetId','userId','status','submittedAt']];for(const d of snap.docs){const x=d.data();rows.push([d.id,x.worksheetId,x.userId,x.status,x.submittedAt?.toDate?.().toISOString?.()??'']);}const csv='\ufeff'+rows.map(r=>r.map(esc).join(',')).join('\n');await audit(a,'EXPORT_REPORT','report',type,{count:snap.size});return {fileName:`report-${type}-${new Date().toISOString().slice(0,10)}.csv`,csv,count:snap.size};});
