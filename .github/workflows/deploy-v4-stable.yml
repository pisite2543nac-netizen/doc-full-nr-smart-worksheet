name: Deploy V4 Stable Full Test
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'stable-test/**'
      - '.github/workflows/deploy-v4-stable.yml'
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: stable-test
      - id: deployment
        uses: actions/deploy-pages@v4
