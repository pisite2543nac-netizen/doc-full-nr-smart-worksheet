import { NavLink,Outlet,useNavigate } from 'react-router-dom';import { BarChart3,BookOpen,ClipboardCheck,FileText,GraduationCap,History,LayoutDashboard,LogOut,Menu,School,Settings,ShieldCheck,Users,X } from 'lucide-react';import { useState } from 'react';import { useAuth } from '../context/AuthContext';
const items=[['/admin','Dashboard',LayoutDashboard],['/admin/users','จัดการผู้ใช้',Users],['/admin/classrooms','จัดการห้องเรียน',School],['/admin/subjects','จัดการรายวิชา',BookOpen],['/admin/worksheets','จัดการใบงาน',FileText],['/admin/grading','ตรวจงาน',ClipboardCheck],['/admin/overrides','สิทธิ์ส่งเกินเวลา',ShieldCheck],['/admin/reports','รายงาน',BarChart3],['/admin/audit','Audit Log',History],['/admin/profile','โปรไฟล์',Settings]] as const;
export function AdminLayout(){const [open,setOpen]=useState(false);const {user,logout,demoMode}=useAuth();const n=useNavigate();async function exit(){await logout();n('/login')}return <div className="min-h-screen bg-slate-50"><aside className={`fixed inset-y-0 left-0 z-40 w-72 border-r border-slate-200 bg-white p-4 transition md:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex items-center justify-between px-2 py-3"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-blue-600 text-white"><GraduationCap/></div><div><b>Smart Worksheet</b><div className="text-xs text-slate-500">Nangrong</div></div></div><button className="md:hidden" onClick={()=>setOpen(false)} aria-label="ปิดเมนู"><X/></button></div><nav className="mt-4 space-y-1">{items.map(([to,label,Icon])=><NavLink key={to} end={to==='/admin'} to={to} onClick={()=>setOpen(false)} className={({isActive})=>`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold ${isActive?'bg-blue-50 text-blue-700':'text-slate-600 hover:bg-slate-50'}`}><Icon size={19}/>{label}</NavLink>)}</nav><button onClick={exit} className="mt-5 flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold text-red-600 hover:bg-red-50"><LogOut size={19}/>ออกจากระบบ</button></aside><main className="md:pl-72"><header className="sticky top-0 z-30 flex h-16 items-center justify-between border-b border-slate-200 bg-white/95 px-4 backdrop-blur md:px-7"><button className="md:hidden" onClick={()=>setOpen(true)} aria-label="เปิดเมนู"><Menu/></button><div className="hidden md:block"><span className="text-sm font-semibold">{user?.displayName}</span><span className="ml-2 text-xs text-slate-500">Admin</span></div>{demoMode&&<span className="badge bg-amber-100 text-amber-800">Demo Mode</span>}</header><div className="p-4 pb-12 md:p-7"><Outlet/></div></main></div>}
