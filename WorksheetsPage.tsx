import { Copy,Edit3,Eye,FileText,Plus,Search } from 'lucide-react';import { Link } from 'react-router-dom';import { StatusBadge } from '../../components/StatusBadge';import { ErrorState,LoadingState } from '../../components/ui';import { useAdminWorksheets } from '../../hooks/useAppData';
export function WorksheetsPage(){const {data:worksheets,loading,error}=useAdminWorksheets();return <div><div className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="page-title">จัดการใบงาน</h1><p className="muted mt-1">Digital / Paper · Draft / Published / Archived</p></div><Link className="btn-primary" to="/admin/worksheets/new"><Plus size={18}/>สร้างใบงาน</Link></div><div className="card mt-6"><div className="relative"><Search className="absolute left-3 top-3 text-slate-400" size={18}/><input className="field pl-10" placeholder="ค้นหาชื่อใบงานหรือรหัสวิชา"/></div></div>{loading?<div className="mt-5"><LoadingState/></div>:error?<div className="mt-5"><ErrorState message={error}/></div>:<div className="mt-5 overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[980px] text-left text-sm"><thead className="bg-slate-50"><tr>{['วิชา','ใบงาน','ประเภท','กำหนดส่ง','สถานะ','ส่งแล้ว','รอตรวจ','จัดการ'].map(h=><th className="px-4 py-3" key={h}>{h}</th>)}</tr></thead><tbody>{worksheets.map((w,i)=><tr className="border-t border-slate-100" key={w.id}><td className="px-4 py-3"><div className="flex items-center gap-2"><span className="h-8 w-1 rounded-full" style={{background:w.subjectColor}}/><b>{w.subjectCode}</b></div></td><td className="px-4 py-3"><div className="font-semibold">{w.title}</div><div className="text-xs text-slate-500">{w.subjectName}</div></td><td className="px-4 py-3"><span className="badge bg-slate-100 text-slate-700"><FileText size={13}/>{w.type}</span></td><td className="px-4 py-3">{w.dueAt?new Date(w.dueAt).toLocaleString('th-TH'):'-'}</td><td className="px-4 py-3"><StatusBadge status={w.status}/></td><td className="px-4 py-3">—</td><td className="px-4 py-3">—</td><td className="px-4 py-3"><div className="flex gap-1"><button aria-label="ดู" className="rounded-lg p-2 hover:bg-slate-100"><Eye size={16}/></button><button aria-label="แก้ไข" className="rounded-lg p-2 hover:bg-slate-100"><Edit3 size={16}/></button><button aria-label="คัดลอก" className="rounded-lg p-2 hover:bg-slate-100"><Copy size={16}/></button></div></td></tr>)}</tbody></table></div></div>}</div>}
